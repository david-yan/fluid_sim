#pragma once

#include "vector2f.hpp"

const Vector2f GRAVITY(0.f, 9.8);
const Vector2f DAMPING(0.9, 0.9);
const float MASS(1000);

const float MAX_VELOCITY(100);

const float DENSITY_SCALE = 16;

const float TARGET_DENSITY(1);
const float PRESSURE_MULTIPLIER(5000);